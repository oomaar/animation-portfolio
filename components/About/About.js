import { Section } from "../../GlobalStyle";
import {
    Container,
} from "./styledAbout";

export const About = () => {
    return (
        <Section>
            <Container>
                About
            </Container>
        </Section>
    );
};
